__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
build/
dist/
.coverage
data/
config/config.yaml
config/.env
.env
*.db
*.db-wal
*.db-shm
*.log
.DS_Store
# Phase 6 / cutover artifacts
library/
config/bookorbit.json
*.py.bak

# deploy/.env is a symlink to ../.env so docker compose finds secrets
# next to the compose file. Track the symlink (not the target) so this
# does NOT need to be recreated on a fresh clone.
!deploy/.env
