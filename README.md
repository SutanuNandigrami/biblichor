# endless-library

Self-hosted Goodreads/Hardcover -> Anna's Archive -> Kindle automation.

See `docs/superpowers/specs/2026-05-17-anna-archive-automation-design.md` for the design.

## Quick start (dev)

```bash
python3.12 -m venv .venv && . .venv/bin/activate
pip install -e ".[dev]"
pytest
```
